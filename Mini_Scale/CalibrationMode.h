#pragma once

// ================================================================
// CalibrationMode.h — режим ручной калибровки тензодатчика
// ================================================================
// Вход в режим: нажать кнопку в течение CAL_ENTRY_WINDOW_MS (1 сек) после старта.
//
// Управление одной кнопкой:
//   Короткое нажатие (< CAL_LONG_PRESS_MS) — изменить cal_factor на шаг текущего режима
//   Длинное нажатие  (>= CAL_LONG_PRESS_MS) — перейти к следующему режиму (wrap-around)
//
// 7 режимов: +10, -10, +1, -1, +0.1, -0.1, SAVE
//
// При SAVE: cal_factor сохраняется в EEPROM и выполняется ESP.restart().
// При таймауте (CAL_IDLE_TIMEOUT_MS) или critical battery: ESP.restart()/deepSleep(0).

// Запуск режима калибровки.
// БЛОКИРУЮЩАЯ ФУНКЦИЯ — управление не возвращается до перезагрузки или deepSleep.
// Не требует return в setup() — используйте как финальный вызов.
void RunCalibrationMode();
